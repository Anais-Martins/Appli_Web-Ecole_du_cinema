/*
const mysql = require('mysql2');

const connection = mysql.createConnection({
  host: '127.0.0.1',
  user: 'root',
  password: '', // ou ton mot de passe si défini
  database: 'university',
  port: 3306
});

connection.connect(err => {
  if (err) {
    console.error('❌ Impossible de se connecter :', err);
  } else {
    console.log('✅ Connecté à MySQL !');
  }
  connection.end();
});
*/